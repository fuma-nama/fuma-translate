## Fuma Translate

A tiny package for UI translations.

It uses a Rust-based compiler to generate types & static info for translation labels, ensuring your UI translations are always up-to-date.

Documentation: https://translate.fuma-nama.dev.
