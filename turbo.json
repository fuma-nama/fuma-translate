{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".vercel", "dist", "native"]
    },
    "types:check": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
