{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist", "packages/core/native/**"]
    },
    "types:check": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
